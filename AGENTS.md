# ZAIHAI 项目维护规则

## 数据保护

- 不得删除或重置生产数据。订单、访客、支付通知、退款、物流、邮件日志、CMS 内容、促销、评价和审计记录都必须通过现有持久化接口读写。
- 不得用 mock 数据覆盖真实后台页面。没有真实数据时显示空状态。
- 修改后台功能前先确认对应数据源和 API 权限，所有后台 API 必须执行服务端登录校验。

## 技术栈

- Next.js App Router + TypeScript。
- 生产部署在 Vercel。
- 核心持久化封装在 `src/lib/durableStore.ts`。
- 零售订单和客户行为数据在 `src/lib/commerceStore.ts`。
- 后台商品、分类、媒体、内容和设置在 `src/lib/backendStore.ts`。
- 后台增量数据如促销、评价、审计在 `src/lib/adminExtraStore.ts`。

## 后台原则

- 后台界面使用简体中文。
- 导航层级控制在两级以内。
- 新增页面必须读取真实数据源或新增持久化数据源。
- 表格默认分页，避免一次性渲染过多实时数据。
- 敏感操作如退款、支付、物流、设置变更需要审计日志。

## 验证

- 修改完成后至少运行 `npm run lint` 和 `npm run build`。
- 部署前确认工作区没有非预期数据文件变更。
